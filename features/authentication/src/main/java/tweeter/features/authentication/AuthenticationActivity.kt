package tweeter.features.authentication

/**
 * Activity that holds the nav host fragment within which
 * fragment destinations in the authentication module can be swapped in and out.
 *
 * @author Seni Joshua
 */
class AuthenticationActivity {
}
